(define (domain noaa_14853_ds1)
  (:requirements :strips :typing :fluents :durative-actions :numeric-fluents)

  (:types
    
  )

  (:predicates
    (precondition_for_unknown)
    (outcome_unknown)
  )

  (:functions
    ; e.g. (duration_spent)
  )

  
  (:action conducting_operations_24_hours/day_exploration_within_several_marine_protected_areas_utilizing_ship's_deep_water_mapping_systems_(kongsberg_em302_multibeam_sonar,_ek60_split-beam_fisheries_sonars,_acoustic_doppler_current_profilers_[adcp],_and_knudsen_3260_chirp_sub-bottom_profiler_sonar)_using_noaa's_two-body_6000_m_remotely_operated_vehicles_(rovs_deep)_unfortunately,_the_provided_context_does_not_specify_the_specific_duration_or_success/failure_details_of_each_task
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

)