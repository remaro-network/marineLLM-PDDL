(define (domain noaa_17271_ds1)
  (:requirements :strips :typing :fluents :durative-actions :numeric-fluents)

  (:types
    
  )

  (:predicates
    (precondition_for_unknown)
    (outcome_unknown)
  )

  (:functions
    ; e.g. (duration_spent)
  )

  
  (:action identifying_and_exploring_the_diversity_of_benthic_habitats_in_the_region,_including_seeps,_deep_corals,_canyons.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

  (:action locating_and_characterizing_underwater_cultural_heritage_(uch),_including_shipwrecks,_to_assess_their_eligibility_for_the_national_register_of_historic_places.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

  (:action testing_the_em_302_acoustic_ability_to_detect_gas_seeps_and_measure_flux_to_understand_its_ability_to_explore_the_water_column.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

  (:action collecting_em_302_bathymetric,_seabed_backscatter,_and_water_column_backscatter_data_for_rov_dive_planning.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

  (:action providing_ek_60_water_column_backscatter_data_for_rov_dive_planning.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

  (:action collecting_knudsen_sub-bottom_data_as_necessary.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

  (:action conducting_expendable_bathythermograph_(xbt)_casts_every_2_to_4_hours_or_more_often_as_necessary_for_mapping_operations.
    :parameters ()
    :precondition (and
      (precondition_for_unknown)
    )
    :effect (and
      (outcome_unknown)
    )
  )

)